//String, Number, Undefined, Null, Boleano, Symbol
const nome = 'Paulo'; //string
const nome1 = 'Paulo'; //string
const nome2 = `Paulo`; //string
const num1 = 10;//number
const num2 = .52;// number
let nomeAluno; //Undefined = não aponta pra local nenhum na memória
const sobrenomeAluno = null;//Nulo -> NÃO apronta pra local nemhum na memoria
const aprovado = false;//false valores boleano = false ou true(lógico)

console.log(typeof aprovado, aprovado);